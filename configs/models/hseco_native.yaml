config:
  hidden_dim: 64
  semantic_heads: 8
  semantic_dropout: 0.3
  node_hidden_dim: 128
  node_heads: 8
  node_dropout: 0.6
  learning_rate: 0.005
  weight_decay: 0.001
  global_threshold: 0.002
  negative_noise_rate: 0.01
  legacy_checkpoint_semantics: false
